<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Config — PRD §9.4
    Production: HTTPS enforced for all remote hosts.
    Development: cleartext allowed only for localhost and the Android emulator host (10.0.2.2)
    so that the local Node.js backend is reachable during development without disabling
    transport security globally.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext to emulator host and localhost only during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
